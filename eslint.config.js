export default {
    "rules": {},
    // empty rules object means "use defaults" 
    "ignores": ["node_modules/"]
    // ignoring node_modules prevents ESLint from trying to check all your dependencies
}